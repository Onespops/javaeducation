package lesson12;

/*
Создать список строк strs с любыми строками внутри
При помощи stream api произвести со строками из strs действия:
  оставить только уникальные строки (метод distinct)
  оставить только строки длиной от 8 до 20 (метод filter)
  отсортировать набор в алфавитном порядке (метод sorted)
Результат собрать (collect() + класс Collectors) в новый список и вывести его в консоль
 */
public class HW1 {

}
